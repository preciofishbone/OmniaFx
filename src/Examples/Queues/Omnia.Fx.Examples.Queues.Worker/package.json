{
  "name": "Omnia.Fx.Examples.Queues.Worker",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -v && tsc && omniafx build",
    "build-release": "npm run build -- --docs --minify"
  },
  "dependencies": {},
  "devDependencies": {
    "gulp": "4.0.2",
    "@omnia/tooling-composers": "6.12.6"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
