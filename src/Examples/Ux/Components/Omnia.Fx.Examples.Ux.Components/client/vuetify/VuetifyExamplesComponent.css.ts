import { types } from 'typestyle';
import { StyleFlow } from '@omnia/fx/ux';

/**
 * Styles for the VuetifyExamplesComponent component
 */
export const VuetifyExamplesComponentStyles = {} as {
    container: types.NestedCSSProperties
};

StyleFlow.define(VuetifyExamplesComponentStyles, {
    container: {
        fontSize: 22
    }
})

