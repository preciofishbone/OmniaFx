{
  "name": "omniafx",
  "version": "1.0.0",
  "scripts": {
    "serve": "start npm run serve:omnia && start npm run serve:vuepress",
    "build": "omniafx build && node omniafx.doc.js && vuepress build docs",
    "serve:vuepress": "omniafx build  --manifestOnly && node omniafx.doc.js dev && vuepress dev docs",
    "serve:omnia": "omniafx serve --no-import"
  },
  "dependencies": {
    "vue": "3.2.33"
  },
  "devDependencies": {
    "@omnia/runtime": "7.0.177-dev",
    "@vitejs/plugin-vue-jsx": "1.3.10",
    "@vuepress/plugin-docsearch": "2.0.0-beta.38",
    "concurrently": "^7.1.0",
    "vuepress": "2.0.0-beta.39"
  }
}