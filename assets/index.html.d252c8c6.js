import{_ as e,g as n}from"./app.a39ec552.js";const a={},o=n(`<h1 id="vs-code-for-omnia-development" tabindex="-1"><a class="header-anchor" href="#vs-code-for-omnia-development" aria-hidden="true">#</a> VS Code for Omnia Development</h1><p>Run the following cmd inside the webapp/worker project folder:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>omnia dev new vscodesetup
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>By default, VS Code disallow <code>Manage Automatic Tasks In Folder</code>, which is also disabled Omnia tasks (i.e. client-side files change detection) . You should ensure to allow it through Command Palette <code>Ctrl + Shift + P</code>. You will need to close and reopen VS Code to apply the changes.</p><p>You can check at the Terminal panel to know if the Omnia automatic tasks is running.</p><p>There are 2 ways to run code:</p><ul><li><p>run or debug in VS Code.</p></li><li><p>run the following cmd inside the project folder</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>omnia dev run
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div></li></ul><h1 id="whatever-editor-for-doing-only-ui-work" tabindex="-1"><a class="header-anchor" href="#whatever-editor-for-doing-only-ui-work" aria-hidden="true">#</a> Whatever editor for doing only UI work</h1><p>Run the following cmd inside the webapp project folder:</p><div class="language-text ext-text line-numbers-mode"><pre class="language-text"><code>omnia dev run
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Done.</p>`,11);function i(d,t){return o}var s=e(a,[["render",i],["__file","index.html.vue"]]);export{s as default};
